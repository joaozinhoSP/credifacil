# Firebase config (set these in Netlify Dashboard)
# FIREBASE_API_KEY, FIREBASE_AUTH_DOMAIN, FIREBASE_PROJECT_ID, etc.

# Webhook secret shared with FortPay to verify webhook authenticity
# Generate a random string and set the same value in FortPay's webhook config
WEBHOOK_SECRET="my-webhook-secret-change-me"
